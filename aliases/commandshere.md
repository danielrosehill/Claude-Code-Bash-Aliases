# Create and Enter Claude Commands Directory

Creates the `.claude/commands` directory in the current working directory and immediately navigates into it.

```bash
alias commandshere='mkdir -p .claude/commands && cd .claude/commands'
```

## Usage

```bash
commandshere
```

This will create the commands directory for Claude Code slash commands and change into it, ready for you to add new slash command files.
