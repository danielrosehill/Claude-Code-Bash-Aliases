# Create and Enter Claude Agents Directory

Creates the `.claude/agents` directory in the current working directory and immediately navigates into it.

```bash
alias agentshere='mkdir -p .claude/agents && cd .claude/agents'
```

## Usage

```bash
agentshere
```

This will create the agents directory for Claude Code custom agents and change into it, ready for you to add new agent configuration files.
